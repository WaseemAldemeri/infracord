# infracord
A declarative, infrastructure-as-code framework for Discord.js. It allows developers to define categories, channels, and permissions as a static "blueprint," ensuring the server structure is always in sync with the code while providing compile-time type safety for all resources.
